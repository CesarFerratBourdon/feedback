source 'https://rubygems.org'
ruby   '2.2.3'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.4'
# Use postgresql as the database for Active Record
gem 'pg'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.3'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer',  platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
# gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0',          group: :doc

# Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
gem 'spring',        group: :development

gem 'puma'

gem 'haml'

gem 'jasny-bootstrap-rails' # nice sliding in menu

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

gem 'bundler'

group :production do
  gem 'rails_12factor'
end

group :development, :test do
  gem 'rspec-rails'
  gem 'factory_girl_rails'
  gem 'letter_opener' # for opening the email
end

group :test do
  gem 'shoulda-matchers'
  gem 'cucumber-rails', require: false
  gem 'database_cleaner'
  gem 'launchy'
  gem 'selenium-webdriver'
  gem 'capybara-webkit'
  gem 'faker'
end

group :development do
  gem 'pry-rails'
  gem 'better_errors'
end

gem 'simple_form'
gem 'devise', github: 'plataformatec/devise'
gem 'activeadmin', github: 'gregbell/active_admin'
gem 'binding_of_caller'
gem 'bootstrap-sass', '3.3.5'
gem 'bootstrap-sass-extras'
gem 'rails_config'
gem 'delayed_job_active_record'
gem 'tzinfo-data'
gem 'validates_formatting_of'

gem 'devise-token_authenticatable', '~> 0.4'

gem 'cells', "~> 4.0.0"
gem "cells-haml"

gem 'cancancan'
gem 'rolify'

gem 'mail'
gem 'mailgun_rails' #for sending mail

gem 'font-awesome-sass', '~> 4.4.0' #for icons

gem "paperclip", "~> 4.3" #for attachments
gem 'aws-sdk-v1' # If using paperclip <= v4.3.1

gem 'pickadate-rails' # for calendar widget

gem 'unread'

gem 'apipie-rails'